-- init.sql
-- This script is executed when the Postgres container is first initialized.
-- The default database "members" is created automatically (via POSTGRES_DB).
-- Here we create the additional database for gym.
CREATE DATABASE gym;
